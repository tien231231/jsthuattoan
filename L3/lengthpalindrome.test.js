
const lengthpalindrome = require("./lengthpalindrome")

test("= ", () => {
  expect(lengthpalindrome("A man a plan a canal Panama" )).toEqual(21);
});
test("= ", () => {
    expect(lengthpalindrome("A man a plan a canal Panama" )).toEqual(21);
  });
  test("= ", () => {
    expect(lengthpalindrome("A man a plan a canal Panama" )).toEqual(21);
  });
  test("= ", () => {
    expect(lengthpalindrome("A man a plan a canal Panama" )).toEqual(21);
  });
  test("= ", () => {
    expect(lengthpalindrome("A man a plan a canal Panama" )).toEqual(21);
  });