//2.1. Write a program that takes a list of numbers as input and returns the second largest number in the list.
/**
 * 
 * @param {Array} arr 
 * @returns 
 */
const Maxinumsum = (arr) => {
    let kq = 0
    for(let i =0;i<arr.length;i++)
    
    return kq ;
  };
  module.exports = maxsecond