
const logestword2tring = require("./logestword2tring")

test("= ", () => {
  expect(logestword2tring("abcdef", "abczyzcdef")).toEqual("cdef")});
  test("= ", () => {
    expect(logestword2tring("abcdef", "abczyzcdef")).toEqual("cdef")});
    test("= ", () => {
      expect(logestword2tring("abcdef", "abczyzcdef")).toEqual("cdef")});
      test("= ", () => {
        expect(logestword2tring("abcdef", "abczyzcdef")).toEqual("cdef")});
        test("= ", () => {
          expect(logestword2tring("abcdef", "abczyzcdef")).toEqual("cdef")});