/**
 * 1.2. Write a program that takes a string as input and displays the length of the string.
 * @param {String} str 
 * @returns 
 */
const leng = (str) => {
    return str.length;
  };


module.exports = leng