/**
 * 1.1.Write a program that takes two numbers as inputs and displays their sum.
 * @param {number} a 
 * @param {number} b 
 * @returns 
 */
//1.1
const sum = (a, b) => {
    return a + b;
  };
  console.log(sum(-3,1));
  module.exports = sum
  